"""
Django settings for smart_class_backend project.

Generated by 'django-admin startproject' using Django 5.2.1.

For more information on this file, see
https://docs.djangoproject.com/en/5.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.2/ref/settings/
"""

from pathlib import Path
import psycopg2.extensions

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-tc2zra+p)b=icx6$0zjy)jy4hvkj=n0-8qd^w&n9wd*&t+v)sa'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',

    # Aplicaciones de terceros
    'rest_framework',
    'corsheaders',
    
    # Aplicaciones propias
    'users',
    'academic',
    'grades.apps.GradesConfig',
    'ml_predictions.apps.MlPredictionsConfig',
    'audit.apps.AuditConfig',  # Nueva aplicación de auditoría
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'audit.middleware.AuditMiddleware',  # Middleware de auditoría (ANTES del NoCache)
    'smart_class_backend.middleware.NoCacheMiddleware',  # Middleware personalizado para deshabilitar caché
]

# Configuración de CORS
CORS_ALLOW_ALL_ORIGINS = True  # Solo para desarrollo
CORS_ALLOW_CREDENTIALS = True
CORS_ALLOWED_HEADERS = [
    'accept',
    'accept-encoding',
    'authorization',
    'content-type',
    'dnt',
    'origin',
    'user-agent',
    'x-csrftoken',
    'x-requested-with',
    'cache-control',
    'pragma',
]

# Configuración de REST Framework
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
}

# Configuración de JWT
from datetime import timedelta

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(days=1),
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),
}

ROOT_URLCONF = 'smart_class_backend.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'smart_class_backend.wsgi.application'


# Database - CONFIGURACIÓN MEJORADA
# https://docs.djangoproject.com/en/5.2/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'smart_class_db2',
        'USER': 'postgres',  # Cambia según tu configuración
        'PASSWORD': 'root',  # Cambia según tu configuración
        'HOST': 'localhost',
        'PORT': '5432',
        'OPTIONS': {
            'isolation_level': psycopg2.extensions.ISOLATION_LEVEL_READ_COMMITTED,
        },
        'CONN_MAX_AGE': 0,  # Deshabilitar pooling de conexiones para desarrollo
    }
}


# Password validation
# https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.2/howto/static-files/

STATIC_URL = 'static/'
STATIC_ROOT = BASE_DIR / 'staticfiles'

# Default primary key field type
# https://docs.djangoproject.com/en/5.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Agregar al final del archivo
AUTH_USER_MODEL = 'users.User'

# ===== CONFIGURACIÓN DE CACHÉ DESHABILITADA PARA DESARROLLO =====
CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.dummy.DummyCache',
    }
}

# ===== CONFIGURACIÓN DE SESSION =====
SESSION_CACHE_ALIAS = 'default'
SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
SESSION_COOKIE_AGE = 86400  # 24 horas

# ===== CONFIGURACIÓN DE LOGGING MEJORADA Y CORREGIDA =====
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': '{levelname} {asctime} {module} {process:d} {thread:d} {message}',
            'style': '{',
        },
        'simple': {
            'format': '{levelname} {message}',
            'style': '{',
        },
        'audit': {
            'format': '[AUDIT] {levelname} {asctime} {message}',
            'style': '{',
        },
    },
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
            'formatter': 'verbose',
            'stream': 'ext://sys.stdout',
        },
        'file': {
            'class': 'logging.FileHandler',
            'filename': 'debug.log',
            'formatter': 'verbose',
            'encoding': 'utf-8',
        },
        'grades_file': {
            'class': 'logging.FileHandler',
            'filename': 'grades.log',
            'formatter': 'verbose',
            'encoding': 'utf-8',
        },
        'audit_file': {
            'class': 'logging.FileHandler',
            'filename': 'audit.log',
            'formatter': 'audit',
            'encoding': 'utf-8',
        },
        'audit_console': {
            'class': 'logging.StreamHandler',
            'formatter': 'audit',
            'stream': 'ext://sys.stdout',
        },
    },
    'loggers': {
        'grades': {
            'handlers': ['console', 'grades_file'],
            'level': 'DEBUG',
            'propagate': False,
        },
        'ml_predictions': {
            'handlers': ['console', 'file'],
            'level': 'INFO',
            'propagate': True,
        },
        'audit': {
            'handlers': ['audit_console', 'audit_file'],
            'level': 'INFO',
            'propagate': False,
        },
        'django.db.backends': {
            'handlers': ['console'],
            'level': 'INFO',
            'propagate': False,
        },
        'django': {
            'handlers': ['console'],
            'level': 'INFO',
        },
    },
    'root': {
        'handlers': ['console'],
        'level': 'WARNING',
    },
}

# ===== CONFIGURACIÓN ESPECÍFICA DE AUDITORÍA =====

# Configurar qué acciones auditar automáticamente
AUDIT_SETTINGS = {
    # Auditar automáticamente estas rutas
    'AUTO_AUDIT_PATHS': [
        '/api/users/',
        '/api/academic/',
        '/api/grades/',
        '/api/ml/',
    ],
    
    # No auditar estas rutas (para evitar spam)
    'SKIP_AUDIT_PATHS': [
        '/api/users/token/refresh/',
        '/admin/jsi18n/',
        '/static/',
        '/media/',
        '/api/audit/',  # Evitar recursión
        '/favicon.ico',
    ],
    
    # Auditar solo métodos importantes
    'AUDIT_METHODS': ['POST', 'PUT', 'PATCH', 'DELETE'],
    
    # Auditar algunos GETs específicos
    'AUDIT_GET_PATHS': [
        '/api/users/users/',
        '/api/grades/grades/stats/',
        '/api/ml/predictions/',
        '/api/academic/classes/',
    ],
    
    # Retener logs por días (usado en tareas de limpieza)
    'RETENTION_DAYS': 90,
    
    # Generar resúmenes diarios automáticamente
    'AUTO_GENERATE_SUMMARIES': True,
    
    # Campos sensibles a filtrar en logs
    'SENSITIVE_FIELDS': ['password', 'token', 'secret', 'key', 'auth', 'csrf'],
    
    # Configuración de alertas de seguridad
    'SECURITY_ALERTS': {
        'FAILED_LOGIN_THRESHOLD': 5,  # Alertar después de N intentos fallidos por hora
        'MASS_DELETION_THRESHOLD': 10,  # Alertar si se eliminan N+ registros en 24h
        'SYSTEM_ERROR_THRESHOLD': 20,  # Alertar si hay N+ errores en 24h
    },
    
    # Configuración de retención por tipo de log
    'RETENTION_BY_ACTION': {
        'LOGIN': 30,  # Logins se mantienen 30 días
        'LOGIN_FAILED': 60,  # Intentos fallidos 60 días
        'DELETE': 365,  # Eliminaciones se mantienen 1 año
        'CREATE': 180,  # Creaciones 6 meses
        'UPDATE': 90,   # Actualizaciones 3 meses
        'VIEW': 30,     # Visualizaciones 30 días
    },
}

# ===== CONFIGURACIÓN DE SEGURIDAD ADICIONAL =====

# Configuración para auditoría de seguridad
SECURITY_SETTINGS = {
    'ENABLE_AUDIT_LOGGING': True,
    'LOG_FAILED_REQUESTS': True,
    'LOG_SLOW_QUERIES': True,
    'SLOW_QUERY_THRESHOLD': 1.0,  # segundos
    'MAX_AUDIT_LOG_SIZE': 100,  # MB
    'AUDIT_LOG_ROTATION': True,
}

# ===== CONFIGURACIÓN DE RENDIMIENTO =====

# Optimizaciones para la aplicación de auditoría
AUDIT_PERFORMANCE = {
    'BATCH_SIZE': 100,  # Procesar logs en lotes de 100
    'ASYNC_LOGGING': False,  # Cambiar a True en producción
    'USE_BULK_CREATE': True,  # Usar bulk_create para mejor rendimiento
    'INDEX_OPTIMIZATION': True,  # Crear índices optimizados
}

# ===== CONFIGURACIÓN PARA PRODUCCIÓN =====

# Configuración que deberías cambiar en producción
if not DEBUG:
    # En producción, usar configuraciones más restrictivas
    AUDIT_SETTINGS.update({
        'RETENTION_DAYS': 365,  # 1 año en producción
        'AUTO_GENERATE_SUMMARIES': True,
    })
    
    # Configuración de logging más restrictiva en producción
    LOGGING['loggers']['audit']['level'] = 'WARNING'
    
    # Habilitar logging asíncrono en producción
    AUDIT_PERFORMANCE['ASYNC_LOGGING'] = True

# ===== CONFIGURACIÓN DE DESARROLLO =====

# Configuraciones específicas para desarrollo
if DEBUG:
    # En desarrollo, mantener menos logs para no llenar el disco
    AUDIT_SETTINGS.update({
        'RETENTION_DAYS': 30,  # Solo 30 días en desarrollo
        'AUTO_GENERATE_SUMMARIES': False,  # No generar resúmenes automáticamente
    })
    
    # Logging más verbose en desarrollo
    LOGGING['loggers']['audit']['level'] = 'DEBUG'
    
    # Agregar handler adicional para debugging
    LOGGING['handlers']['audit_debug'] = {
        'class': 'logging.FileHandler',
        'filename': 'audit_debug.log',
        'formatter': 'verbose',
        'encoding': 'utf-8',
    }
    
    LOGGING['loggers']['audit']['handlers'].append('audit_debug')

# ===== VALIDACIÓN DE CONFIGURACIÓN =====

# Validar que la configuración de auditoría sea correcta
def validate_audit_settings():
    """Validar configuración de auditoría"""
    required_keys = ['AUTO_AUDIT_PATHS', 'SKIP_AUDIT_PATHS', 'RETENTION_DAYS']
    for key in required_keys:
        if key not in AUDIT_SETTINGS:
            raise ValueError(f"Falta configuración de auditoría: {key}")
    
    # Validar que los paths sean listas
    for path_key in ['AUTO_AUDIT_PATHS', 'SKIP_AUDIT_PATHS', 'AUDIT_GET_PATHS']:
        if not isinstance(AUDIT_SETTINGS.get(path_key, []), list):
            raise ValueError(f"La configuración {path_key} debe ser una lista")
    
    # Validar rangos numéricos
    if AUDIT_SETTINGS['RETENTION_DAYS'] < 1:
        raise ValueError("RETENTION_DAYS debe ser mayor a 0")

# Ejecutar validación al importar settings
try:
    validate_audit_settings()
except ValueError as e:
    print(f"Error en configuración de auditoría: {e}")
    if not DEBUG:
        raise  # En producción, fallar si la configuración es incorrecta

# ===== CONFIGURACIÓN DE COMANDOS PERSONALIZADOS =====

# Configuración para comandos de gestión de auditoría
AUDIT_MANAGEMENT = {
    'DEFAULT_EXPORT_LIMIT': 10000,
    'CSV_DELIMITER': ',',
    'CSV_ENCODING': 'utf-8',
    'CLEANUP_BATCH_SIZE': 1000,
    'SUMMARY_GENERATION_HOUR': 2,  # Hora del día para generar resúmenes (2 AM)
}

# ===== CONFIGURACIÓN DE MONITOREO =====

# Configuración para monitoreo y alertas
MONITORING = {
    'ENABLE_HEALTH_CHECKS': True,
    'HEALTH_CHECK_INTERVAL': 300,  # 5 minutos
    'ALERT_EMAIL_FROM': 'admin@smartclass.com',
    'ALERT_EMAIL_TO': ['admin@smartclass.com'],
    'ENABLE_EMAIL_ALERTS': False,  # Cambiar a True cuando tengas SMTP configurado
}

# ===== NOTAS PARA PRODUCCIÓN =====

"""
IMPORTANTE: Antes de ir a producción, asegúrate de:

1. Cambiar SECRET_KEY por una clave segura
2. Establecer DEBUG = False
3. Configurar ALLOWED_HOSTS apropiadamente
4. Configurar base de datos de producción
5. Establecer configuración SMTP para alertas
6. Configurar servidor web (nginx/apache) para servir archivos estáticos
7. Configurar CORS adecuadamente (no usar CORS_ALLOW_ALL_ORIGINS = True)
8. Configurar HTTPS
9. Configurar backup de base de datos
10. Configurar rotación de logs
11. Configurar tareas programadas (cron) para:
    - Generar resúmenes diarios de auditoría
    - Limpiar logs antiguos
    - Verificar alertas de seguridad

Comandos cron sugeridos:
# Generar resumen diario a las 2 AM
0 2 * * * cd /path/to/project && python manage.py audit_maintenance summary

# Verificar alertas cada hora
0 * * * * cd /path/to/project && python manage.py audit_maintenance security_alerts

# Limpiar logs antiguos cada domingo a las 3 AM
0 3 * * 0 cd /path/to/project && python manage.py audit_maintenance cleanup --days 90
"""